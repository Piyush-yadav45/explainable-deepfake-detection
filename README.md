# explainable-deepfake-detection
Most of the deepfake detectors available outputs only 'real' or 'fake' without explaining the
evidence, reducing trust in security applications.
We are trying to build a deepfake detector which gives a reasoning of why it marked a specific video as 'real' or 'fake'.